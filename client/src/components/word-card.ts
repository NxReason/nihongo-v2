import { html, LitElement } from "lit";
import { customElement } from "lit/decorators.js";

@customElement('word-card')
export class WordCard extends LitElement {
    render() {
        return html`Word Card`;
    }
}
