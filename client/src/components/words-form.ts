import { html, LitElement } from "lit";
import { customElement } from "lit/decorators.js";

@customElement('words-form')
export class WordsForm extends LitElement {
    render() {
        return html`Words Form`;
    }
}
